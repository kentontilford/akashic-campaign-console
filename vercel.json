{
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  },
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "npm run build:vercel"
      }
    }
  ]
}